#include "school.h"

/**
 * main -
 *
 * Return: Always 0 (Success)
 */

void main(void)
{
	int a, b;

	for (a = 0; a <= 9; a++)
	{
		for (b = 97; b <= 122; b++)
		{
			_putchar(b);
		}
	_putchar('\n');
	}
}
